{
  "name": "root",
  "private": true,
  "scripts": {
    "module:install": "pnpm install",
    "module:clean": "lerna clean && rm -rf ./node_modules",
    "bump": "lerna version"
  },
  "workspaces": [
    "packages/*",
    "services/*",
    "sites/*"
  ],
  "devDependencies": {
    "@nestjs/cli": "^9.0.0",
    "lerna": "^7.1.5",
    "nx": "^16.7.2"
  }
}
